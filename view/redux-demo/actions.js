var {DECREASE, INCREASE, RESET} = require('./actionTypes');

const decrease = {
    type : DECREASE
};

const increase = {
    type : INCREASE
};

const reset = {
    type : RESET
};

export {
  decrease, increase, reset
}